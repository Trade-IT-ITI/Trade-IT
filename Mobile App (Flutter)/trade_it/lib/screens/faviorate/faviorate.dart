import 'package:flutter/material.dart';


class Faviorate extends StatelessWidget {
  const Faviorate({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children:const [
          Text("Faviorate"),
         
        ],
      ),
    );
  }
}